using Microsoft.UI.Xaml.Controls;

namespace KinematicsCalculator.NET
{
    public sealed partial class AboutDialog : ContentDialog // Added 'partial' keyword
    {
        public AboutDialog()
        {
            this.InitializeComponent();
        }
    }
}