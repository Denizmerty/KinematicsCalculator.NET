<winex:WindowEx
    x:Class="KinematicsCalculator.NET.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KinematicsCalculator.NET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winex="using:WinUIEx"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    mc:Ignorable="d"
    MinWidth="500"
    MinHeight="650">
    <winex:WindowEx.SystemBackdrop>
        <media:MicaBackdrop/>
    </winex:WindowEx.SystemBackdrop>
    <Grid Padding="20" RowSpacing="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
            <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{ThemeResource ControlCornerRadius}"
                    Padding="15">
                <StackPanel Spacing="12">
                    <TextBlock Text="Inputs" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,5"/>
                    <TextBlock Text="Enter 3 known values." Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,10"/>
                    <Grid ColumnSpacing="10" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="110"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBox x:Name="InputDisplacementTextBox" Header="Displacement (Δx)" Grid.Row="0" Grid.Column="0" PlaceholderText="Enter value"/>
                        <ComboBox x:Name="UnitDisplacementComboBox" Grid.Row="0" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,2"/>
                        <TextBox x:Name="InputInitialVelocityTextBox" Header="Initial Velocity (v₀)" Grid.Row="1" Grid.Column="0" PlaceholderText="Enter value"/>
                        <ComboBox x:Name="UnitInitialVelocityComboBox" Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,2"/>
                        <TextBox x:Name="InputFinalVelocityTextBox" Header="Final Velocity (v)" Grid.Row="2" Grid.Column="0" PlaceholderText="Enter value"/>
                        <ComboBox x:Name="UnitFinalVelocityComboBox" Grid.Row="2" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,2"/>
                        <TextBox x:Name="InputAccelerationTextBox" Header="Acceleration (a)" Grid.Row="3" Grid.Column="0" PlaceholderText="Enter value"/>
                        <ComboBox x:Name="UnitAccelerationComboBox" Grid.Row="3" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,2"/>
                        <TextBox x:Name="InputTimeTextBox" Header="Time (t)" Grid.Row="4" Grid.Column="0" PlaceholderText="Enter value"/>
                        <ComboBox x:Name="UnitTimeComboBox" Grid.Row="4" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,2"/>
                    </Grid>
                </StackPanel>
            </Border>
        </ScrollViewer>
        <ComboBox x:Name="CalculateChoiceComboBox"
                  Grid.Row="1"
                  Header="Calculate:"
                  HorizontalAlignment="Stretch"
                  MinWidth="250"
                  SelectionChanged="CalculateChoiceComboBox_SelectionChanged"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
            <Button x:Name="ClearButton" Click="ClearButton_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <SymbolIcon Symbol="Clear"/>
                        <TextBlock Text="Clear"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            <Button x:Name="CalculateButton" Style="{ThemeResource AccentButtonStyle}" Click="CalculateButton_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <SymbolIcon Symbol="Calculator"/>
                        <TextBlock Text="Calculate"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </StackPanel>
        <Border x:Name="ResultBorder" Grid.Row="3"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{ThemeResource ControlCornerRadius}"
                Padding="15"
                Visibility="Collapsed">
            <StackPanel Spacing="5">
                <TextBlock Text="Result" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <TextBlock x:Name="ResultVariableTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ResultValueTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ResultUnitTextBlock" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>
        <InfoBar x:Name="StatusInfoBar" Grid.Row="4" IsOpen="False" IsClosable="True" MinWidth="300" />
    </Grid>
</winex:WindowEx>